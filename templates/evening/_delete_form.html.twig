<form method="post" action="{{ path('app_evening_delete', {'id': evening.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evening.id) }}">
    <button class="btn btn-outline-danger">Delete</button>
</form>
